Create a virtual environment before running the application
virtual environment command : virtualenv (name_of_your_env)

install the requirement libraries using the command below
pip install -r requirements.txt 